# The main pipeline for dab_cicd_demo

resources:
  pipelines:
    dab_cicd_demo_etl:
      name: dab_cicd_demo_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/dab_cicd_demo_etl"

      libraries:
        - glob:
            include: ../src/dab_cicd_demo_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
